# random canvas fingerprint
randomcanvas.js application/javascript
(function() {
	return Object.defineProperty(
		HTMLCanvasElement.prototype,
		'toDataURL', { 
			value: function() {
				const sample = "pVBORw0KGgoAA"
				const randomized = sample.split('').sort(() => 0.5-Math.random()).join('')
				const dataURL = 'data:image/png;base64,'+randomized
				console.warn('🐿 HTMLCanvasElement.toDataURL accessed')
				return dataURL
			}
		}
	)
})()
